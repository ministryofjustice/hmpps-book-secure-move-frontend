{{ appTimelineItemHeading(params, item) }}

<div class="app-timeline__content">
  {{ appTimelineItemDescription(params, item) }}
  {{ appTimelineItemDate(params, item) }}
  {{ appTimelineItemAdditional(params, item) }}
</div>
