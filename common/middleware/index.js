const setLocationItems = require('./set-location-items')
const setMoveSummary = require('./set-move-summary')
const setMoveSummaryWithSessionData = require('./set-move-summary-with-session-data')
const uniqueFormWizard = require('./unique-form-wizard')

module.exports = {
  setLocationItems,
  uniqueFormWizard,
  setMoveSummary,
  setMoveSummaryWithSessionData,
}
