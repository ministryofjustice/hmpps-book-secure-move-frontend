{% extends "layouts/govuk.njk" %}

{% block content %}
  <h1 class="govuk-heading-xl">{{ translate(statusMessage) }}</h1>

  {% if statusCode == '404' %}
    <p>{{ translate("errors.not_found.content") }}</p>
  {% elseif statusCode == '403' and error.code === 'EBADCSRFTOKEN' %}
    <p>{{ translate("errors.tampered_with.content") }}</p>
  {% else %}
    <p>{{ translate("errors.default.content") }}</p>
  {% endif %}

  {% if showStackTrace %}
    <dl class="app-stack-trace">
      <dt class="app-stack-trace__heading">{{ statusCode }} - {{ error.message }}</dt>
      <dd class="app-stack-trace__details">{{ error.stack }}</dd>
    </dl>
  {% endif %}

{% endblock %}
