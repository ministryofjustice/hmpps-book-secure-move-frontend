const renderAssessments = require('./render-assessments')
const renderDetails = require('./render-details')
const renderPersonalDetails = require('./render-personal-details')
const renderRequestInformation = require('./render-request-information')
const renderTimeline = require('./render-timeline')
const renderWarnings = require('./render-warnings')

module.exports = {
  renderAssessments,
  renderDetails,
  renderPersonalDetails,
  renderRequestInformation,
  renderTimeline,
  renderWarnings,
}
