import { cancellationReason } from './cancellation-reason'
import { cancellationReasonCancelledByPMUComment } from './cancellation-reason-cancelled-by-pmu-comment'

/* eslint-disable camelcase */
export const cancellation_reason = cancellationReason
export const cancellation_reason_cancelled_by_pmu_comment = cancellationReasonCancelledByPMUComment
