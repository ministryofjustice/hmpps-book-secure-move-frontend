const confirmation = require('./confirmation')
const journeys = require('./journeys')
const previewOptIn = require('./preview-opt-in')
const previewOptOut = require('./preview-opt-out')
const timeline = require('./timeline')
const view = require('./view')

module.exports = {
  confirmation,
  journeys,
  previewOptIn,
  previewOptOut,
  timeline,
  view,
}
