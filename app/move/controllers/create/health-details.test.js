describe('the workaround in the health details controller', () => {
  describe('render', () => {
    it('removes from the rendered fields the special vehicle option, as it\'s dealth with elsewhere', () => {

    })
  })
  describe('save values', () => {
    it('takes the value of the additional field and treats it as an item in health', () => {

    })
    it('copies the value of type of vehicle and puts it as comment', () => {

    })
  })
})
